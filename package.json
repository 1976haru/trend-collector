{
  "name": "trend-collector",
  "version": "1.1.0",
  "private": true,
  "description": "전국 언론보도 자동 수집 + 요약 + PDF/메일 발송 시스템",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.8.0",
    "@emailjs/browser": "^3.11.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.10",
    "gh-pages": "^6.0.0"
  }
}
